# No Build Guty Examples

Here are some examples of setting up Gutenberg blocks without a build step.

*note:* for the `test-block-with-htm` example you still will need to npm install to get the htm dependency.